# 운영 매뉴얼 — 매니저 매치 이동 가설 검증 테스트

> **대상**: 운영자
> **연관 문서**: [PRD](./PRD-매니저매치이동테스트.md)
> **버전**: v1.2 (2026-05-21) — 채널톡 복붙 발송 방식 (ADR-013)

이 시스템은 메시지를 **자동 발송하지 않습니다.** 운영자가 발송 관리 화면에서 대상자별 메시지를 복사해 **채널톡에서 1:1로 발송**하고, 매니저의 이동 요청을 PLAB 어드민에서 수동 처리합니다.

---

## 0. 운영자가 하는 일 (한눈에)

| 빈도 | 작업 | 화면 |
|---|---|---|
| 시작 1회 | 기준값·프로모션 금액 설정 | `/admin/config` |
| 발송 주기마다 | 대상자별 메시지 복사 → 채널톡 발송 → 완료 처리 | `/admin/export` |
| 슬랙 알림 수신 시 | 매니저 변경 처리 → 이모지 마킹 | Slack + PLAB 어드민 |
| 수시 | Funnel 지표 확인 | `/admin/funnel` |

화면 주소(로컬 기준):
- 운영자 설정: `http://localhost:3000/admin/config`
- 발송 관리: `http://localhost:3000/admin/export`
- Funnel 리포트: `http://localhost:3000/admin/funnel`

> 배포 후에는 도메인이 바뀝니다(예: `https://test.plab.com/admin/...`).

---

## 1. 발송 절차 (F-3) — ⭐ 핵심 루틴

시스템은 매시 정각에 "3시간 뒤 시작하는 진행가능성 낮은 매치"의 매니저를 추출해 **발송 대기(pending)** 로 쌓아둡니다. 운영자는 이를 주기적으로 채널톡으로 발송합니다.

> 💡 **추출 알림**: 매시 배치가 신규 대상자를 추출하면 슬랙 `#match-move-test`에 **📣 대상자 추출 알림**(신규 건수 + 발송 관리 링크)이 옵니다. 이 알림을 받으면 발송 관리 화면을 열어 발송하세요. (신규 0건이면 알림이 오지 않습니다.)

### 1-1. 타이밍 규칙 (중요)
- 매니저는 **매치 시작 1시간 30분 전**까지만 추천 페이지에서 이동/유지를 선택할 수 있습니다.
- 따라서 발송은 **매치 시작 1시간 30분 전보다 충분히 일찍** 끝나야 합니다.
- 권장: 발송 관리 화면을 **2~3시간 간격**(또는 매시간) 확인하여 대기분을 즉시 발송.

### 1-2. 단계
1. `/admin/export` 접속 → 상단 **"발송 대기 N건"** 확인. 대상자별 카드가 목록으로 표시됩니다.
2. 각 카드에서:
   - **[번호 복사]** → 채널톡에서 해당 전화번호로 매니저(대화)를 찾습니다.
   - **[메시지 복사]** → 채널톡 대화창에 붙여넣고 발송합니다.
     - 메시지에는 안내 문구 + 추천 페이지 링크(`▶ 추천 매치 보기: …`)가 포함되어 있습니다.
     - 메시지 본문에는 전화번호가 들어가지 않습니다(매니저에게 노출 안 됨). 번호는 화면에서 대상 식별용으로만 표시됩니다.
3. 발송한 카드의 **[발송 완료]** 를 누릅니다.
   - 해당 대상이 `exported`로 전환되고 Funnel의 "발송 완료" 지표가 올라갑니다.
   - 여러 건을 한 번에 보냈다면 상단 **[전체 발송 완료 처리]** 를 사용해도 됩니다.
   - ⚠️ **실제로 발송한 뒤에만** 누르세요. 보기만 하고 누르면 지표가 왜곡됩니다.

### 1-3. 주의
- 발송 완료 처리를 깜빡하면 다음에도 같은 대상이 목록에 남습니다(중복 발송 위험) — 발송 후 반드시 완료 처리.
- 같은 매니저+매치는 중복 추출되지 않습니다(시스템이 자동 제거).
- 카드에 "번호 조회 실패"가 표시되면 PLAB 연락처 조회가 일시 실패한 것입니다. 잠시 후 **새로고침**하세요. 지속되면 개발자 문의.

---

## 2. 이동 요청 처리 (F-6) — 슬랙 알림 대응

매니저가 추천 페이지에서 액션하면 `#match-move-test` 채널에 알림이 옵니다.

### 2-1. "매치 이동 요청" 알림을 받으면
```
🔔 매치 이동 요청 / {시각}
매니저: {이름} (ID: {id})
현재 매치: {구장} / {시각} / 참가자 {n}명
요청 매치: {구장} / {시각} / 참가자 {n}명
양도 매물 여부: {YES/NO}
프로모션 매치 여부: {test_type or NONE}
```
처리 순서:
1. **PLAB 어드민에서 요청 매치의 현재 상태 재확인** (매니저 미배정·정원 등 변동 가능).
2. **매니저 변경 처리** (현재 매치 → 요청 매치).
3. 요청 매치가 **양도 매물(양도 매물 여부 = YES)** 이면 → **프로모션 수동 해제**.
4. 매니저에게 **완료 안내** (채널톡 등으로 직접).
5. 슬랙 알림 메시지에 **`:white_check_mark:` (✅) 이모지**를 추가.
   - 시스템이 이를 webhook으로 수신해 `change_completed` 이벤트를 기록합니다(완료율 지표 반영).
   - 이모지는 **한 번만** 추가하면 됩니다(중복 처리는 무시됨).

### 2-2. "유지 선택" 알림을 받으면
- 단순 정보 알림입니다. **운영자 처리 불필요**.

---

## 3. 설정 관리 (F-1 / F-1B) — `/admin/config`

### 3-1. 진행가능성 기준값 (F-1)
- **낮음 기준**: 이 인원 **미만**이면 "진행 가능성 낮음" → 발송 대상. 초기값 4.
- **높음 기준**: 이 인원 **이상**인 매치만 추천 후보. 초기값 6.
- 낮음 ≥ 높음으로는 저장할 수 없습니다(검증). 변경 시 이력이 남습니다.

> **튜닝 팁**: 추천 매치가 너무 적게 잡히면(발송 대기가 안 쌓이면) 높음 기준을 낮춰보세요(예: 6→5).

### 3-2. 프로모션 금액 매핑 (F-1B)
- `test_type` 3·6·7·8·9별 환산 금액(원)을 입력 → 비용 절감 지표 계산에 사용. 테스트 시작 전 반드시 입력.

---

## 4. 리포트 확인 (F-8) — `/admin/funnel`

Funnel 단계: 추출됨 → **발송 완료** → 페이지 진입 → 변경 요청 → 유지 선택 → 운영자 처리 완료

- **이동 요청률 = 변경 요청 ÷ 발송 완료**.
- **완료율 = 운영자 처리 완료 ÷ 변경 요청**.
- 비용 절감: 양도+프로모션 이동 완료 건수 × test_type별 환산 금액.

---

## 5. 트러블슈팅

| 증상 | 확인/조치 |
|---|---|
| `/admin/export`에 "발송 대기 중인 대상이 없습니다" | 정상(대기 0건). 다음 정각 추출 대기 또는 기준값 조정. |
| 카드에 "번호 조회 실패" | PLAB 연락처 조회 일시 실패. 새로고침 후 재시도. 지속 시 개발자 문의. (메시지 복사는 가능) |
| [복사] 버튼을 눌러도 복사가 안 됨 | 브라우저 클립보드 권한 문제. https 또는 localhost에서 접속했는지 확인. 안 되면 메시지 박스를 직접 드래그 복사. |
| [발송 완료]를 눌렀는데 0건 처리 | 이미 처리되었거나 그 사이 상태가 바뀜. 새로고침 후 재확인. |
| 슬랙 알림이 오지 않음 | webhook URL/시그니처 설정 확인(개발자). |
| 이모지를 눌렀는데 완료 지표가 안 오름 | 발신 메시지에 `#target-{id}` 식별자가 있어야 매핑됨. 같은 메시지의 스레드/본문 확인. |

---

## 6. 보안·데이터 취급

- 발송 관리 화면에 표시되는 **전화번호는 식별용**이며, 시스템 자체 DB에는 저장하지 않습니다(발송 시점 PLAB 조회만).
- 복붙 메시지 본문에는 전화번호가 포함되지 않습니다(매니저에게 노출 안 됨).
- 추천 페이지 링크(토큰)는 매치 시작 1.5시간 전 만료됩니다.
- 테스트 종료·분석 완료 후 자체 DB는 폐기 대상입니다(PRD §5.3).

---

## 7. 일일 운영 체크 (요약)

- [ ] (시작일) 기준값·프로모션 금액 설정 완료
- [ ] 발송 주기마다 `/admin/export`에서 대기분 복사→채널톡 발송→발송 완료 처리
- [ ] 슬랙 "이동 요청" 알림 → 매니저 변경 → (양도 매물이면) 프로모션 해제 → ✅ 이모지
- [ ] 수시로 `/admin/funnel` 지표 확인
